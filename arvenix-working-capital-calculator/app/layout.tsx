import "./globals.css"; import type { Metadata } from "next"; export const metadata:Metadata={title:"Working Capital Calculator | Arvenix",description:"Estimate inventory working capital release and illustrative financing benefit."}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}