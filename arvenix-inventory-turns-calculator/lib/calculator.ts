export interface Inputs{annualMaterialCogs:number;averageInventory:number;targetTurns:number} export const defaultInputs:Inputs={annualMaterialCogs:6600000,averageInventory:3000000,targetTurns:2.75}; export function calculate(i:Inputs){const currentTurns=i.averageInventory>0?i.annualMaterialCogs/i.averageInventory:0;const daysOnHand=currentTurns>0?365/currentTurns:0;const targetInventory=i.targetTurns>0?i.annualMaterialCogs/i.targetTurns:0;const workingCapitalReleased=Math.max(0,i.averageInventory-targetInventory);const targetDaysOnHand=i.targetTurns>0?365/i.targetTurns:0;return{currentTurns,daysOnHand,targetInventory,workingCapitalReleased,targetDaysOnHand}}