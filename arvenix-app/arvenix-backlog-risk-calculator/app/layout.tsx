import "./globals.css"; import type { Metadata } from "next"; export const metadata:Metadata={title:"Backlog Revenue Risk Calculator | Arvenix",description:"Estimate revenue and contribution exposure from aging backlog."}; export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}